# Auth (required)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Turso (production / demo — optional, falls back to local SQLite)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Local Prisma migration database (SQLite)
DATABASE_URL="file:./dev.db"
