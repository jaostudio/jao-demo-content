# Mobile Visual Audit — 2026-05-30

Scope
- Focus: perception-first mobile presentation (primary viewport widths: 360, 390, 412). Pages: Home (Hero), Projects, Services, Process, Contact.
- Owner for fixes: .opencode (they will implement design changes and larger restructures).

Summary
- Overall: site is visually strong on desktop but has a few mobile-specific polish issues that reduce perceived hierarchy and usability. I recommend low-risk, high-ROI fixes first (touch targets, CTA placement, clipping), then medium-risk layout adjustments (hero simplification, card content reserve).

High-priority issues (P1) — block or harm usability

- Contact submit CTA clipped on some devices before fix
  - Symptom: the primary submit CTA can be partially off-screen or clipped by parent overflow/transform; users may miss it.
  - Evidence: captured at 360×800; CTA sat at bottom edge and was partially outside the visible masked area.
  - Immediate mitigation implemented: portal-mounted fixed submit button (code hotfix in `src/components/sections/contact-section.tsx`) to avoid clipping. Recommend .opencode to evaluate a design-consistent sticky CTA or flush full-width CTA with safe insets.

- Touch targets smaller than guideline in a few inputs
  - Symptom: some inputs were using `py-3` (≈40px) on mobile; increased to `py-4` (~44–48px) in a targeted patch. Verify across devices.
  - Recommendation: standardize inputs to min 44px height on mobile and audit stacked spacing to avoid overlaps with fixed CTAs.

Medium-priority issues (P2) — reduces perceived quality

- Hero node-graph competes with copy on small viewports
  - Symptom: animated node graph draws attention away from heading and CTAs; on very narrow devices it can make the hero feel crowded.
  - Fix applied: compact static node graph for mobile via `compact` prop. Recommend design review: reduce visual noise, increase whitespace above/below copy, and ensure H1 remains primary visual anchor.

- Project card height variance (visual rhythm)
  - Symptom: cards varied in height causing a staggered grid effect and uneven rhythm on mobile lists.
  - Fixes applied: grid row template + min-heights reduced variance to ≈4.5%. Recommend additional content reserve (fixed summary min-height) and microcopy trimming to keep cards aligned.

Low-priority issues (P3) — cosmetic / optional

- CTA pill + left icon can appear cropped on extreme narrow widths
  - Suggestion: on ≤360px, make CTA full-width flush with page (no side insets), reduce left icon to a small spacing, or hide icon entirely.

- Minor typographic scaling at 360 vs 412 — headings feel dense at smallest width
  - Suggestion: minor font-size scale or line-height tweak for H1 at ≤360px.

Suggested deliverables for .opencode (implementation-ready)

1. Apply a design-consistent mobile CTA pattern:
   - Option A (preferred): full-width sticky CTA with safe insets (x:0) and interior padding. Ensure it is portal-mounted to avoid clipping.
   - Option B: sticky bottom sheet with clear affordance (transparent over content, with visible form close/expand control).

2. Standardize input touch-targets and spacing:
   - Inputs/selects/textarea: `min-height: 44px` at mobile breakpoints.
   - Form container: add bottom padding equal to CTA height + safe area (e.g., `pb-[calc(56px+env(safe-area-inset-bottom))]`).

3. Hero simplification & spacing:
   - Keep compact node-graph for mobile (static, fewer nodes) and increase top/bottom whitespace around hero copy/CTAs.
   - Ensure h1 bounding box and primary CTA appear above mid-fold at standard handset heights.

4. Projects card layout:
   - Keep grid rows with reserved content area; set card summary to a fixed two-line clamp with consistent min-height.

5. QA checklist for each change (required before merge):
   - Screenshots at 360×800, 390×844, 412×915 for affected pages.
   - Interactive check: confirm fixed CTA does not block inputs; confirm focus/keyboard behavior is preserved.
   - Performance: ensure changes don't reintroduce layout thrashing or large repaints.

Artifacts
- Screenshots and boards: `../../.opencode/screenshots/` and `../boards/` (use existing artifacts where present). Link screenshots in PR.
- Code hotfix: `src/components/sections/contact-section.tsx` (portal-mounted CTA + input padding patch). See commit on branch `fix/projects-card-consistency`.

Next steps I can take
- Run an automated mobile screenshot sweep for the pages listed, produce annotated boards, and add per-issue screenshots to `../boards/` and `../annotations/`.
- Open a PR with this report and the artifacts for .opencode to pick up.

If you want I will now run the full mobile screenshot sweep and attach per-page boards for .opencode to act on. Confirm and I'll start.

— Generated by automated audit (assistant)
